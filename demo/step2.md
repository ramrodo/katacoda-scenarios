Clone the Meshery repository:

`git clone https://github.com/layer5io/meshery.git` {{copy}}

Install the UI dependencies:

`make setup-ui-libs` {{copy}}

Build and export the UI:

`make build-ui` {{copy}}






















